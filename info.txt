# Para pruebas locales en Mac:
npm run dev-mac

# Para generar el instalador de Windows
npm run build-all

# Para subir tus actualizaciones a GitHub
npm run git-release


NEW release

# Sincroniza primero el cambio de versión en tu repositorio remoto
git push origin main

# Genera la compilación para Windows y la sube automáticamente como Draft a GitHub
export GH_TOKEN=tu-token-de-git-aqui
npm run release-win

# Para subir tus actualizaciones a GitHub
# Para subir tus actualizaciones a GitHub
export GH_TOKEN=tu-token-de-git-aqui
npm run git-release
